<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
      <h5 class="my-0 mr-md-auto font-weight-normal">
        <a href="{{ path('homepage') }}">My Blog</a> 
      </h5>
      <nav class="my-2 my-md-0 mr-md-3">
      <a class="p-2 text-dark" href="#">Ubersicht</a>
      {% if app.user %}
        <a class="p-2 text-dark" href="{{ path('app_blog_add') }}">Neuer Eintrag</a>
      {% endif %}
        <a class="p-2 text-dark" href="#">Impressum</a>
      </nav>
      {% if app.user %}
        <a class="btn btn-outline-primary" href="{{ path('app_logout') }}">Logout</a>
      {% else %}
        <a class="btn btn-outline-primary" href="{{ path('app_login') }}">Login</a>
      {% endif %}
      
</div>